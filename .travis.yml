language: python

python:
    - "2.7"

addons:
    apt:
        packages:
            - gfortran

install:
    - pip install --upgrade pip setuptools wheel
    - pip install -r requirements.txt

script:
    - man gfortran
